# ====================================================================
# SCL Institute - Production Environment Configuration
# ====================================================================
# IMPORTANT: Never commit this file to git
# This is a TEMPLATE - copy to .env.production on production server
# Update with your actual production values
# ====================================================================

# ---- Database Configuration ----
# On production server, use the Docker container hostname
DB_HOST=scli-mysql
DB_USER=scl_user
DB_PASSWORD=SclSecurePass2024!
DB_NAME=scl_institute
DB_PORT=3306

# ---- Moodle Configuration ----
# Production: Use the actual server IP and port
# IMPORTANT: No path suffix like /moodle-prod
# Moodle is installed at the root of port 8888
MOODLE_URL=http://185.211.6.60:8888
MOODLE_DATABASE_HOST=185.211.6.60
MOODLE_DATABASE_USER=root
MOODLE_DATABASE_PASSWORD=RootSecurePass2024!
MOODLE_DATABASE_NAME=moodle

# ---- Backend Configuration ----
BACKEND_PORT=4000
BACKEND_DEBUG=false
VITE_API_URL=http://185.211.6.60/api

# ---- Frontend Configuration ----
FRONTEND_PORT=3000
VITE_BASE_URL=/

# ---- JWT/SSO Configuration ----
SSO_SECRET=supersecretkey
JWT_EXPIRY=24h

# ---- Logging ----
LOG_LEVEL=warn
NODE_ENV=production
